import React, {FunctionComponent} from 'react';

interface OwnProps {
}

type Props = OwnProps;

const LocationItem: FunctionComponent<Props> = (props) => {

    return (
        <div>Hola</div>
    );
};

export default LocationItem;
